I am uploading my COMPLETE existing project ZIP.

YOUR JOB IS TO RETURN A COMPLETE, PRODUCTION-READY PROJECT ZIP.

I do NOT want a patch.
I do NOT want selected files.
I do NOT want an incomplete ZIP.
I do NOT want instructions telling me to manually edit files.

I want ONE FINAL ZIP containing the ENTIRE APPLICATION, ready to upload to GitHub and deploy to Vercel.

==================================================
1. ABSOLUTE PRESERVATION RULE
==================================================

The uploaded ZIP is the source of truth.

You MUST preserve the entire existing application.

Do not delete, omit, replace, simplify, redesign, or remove anything unless a change is absolutely required to make the application work.

The final ZIP must contain ALL existing project files and functionality, including but not limited to:

- entire Python learning game
- every Python level
- every mission
- every challenge
- game progression
- XP/rewards
- editor
- code execution
- portfolio
- gigs
- career system
- saves
- cloud saves
- sync
- game state
- UI
- screens
- components
- routes
- layouts
- assets
- styles
- icons
- configuration
- database code
- API routes
- authentication
- session handling
- all existing features

DO NOT make a smaller version of the project.

The final ZIP must be a COMPLETE replacement for the uploaded project.

==================================================
2. AUTHENTICATION
==================================================

Fix the authentication system completely.

I want:

Google login
Google logout
persistent sessions
session restoration after refresh
protected user data
correct callback handling
correct cookies
correct production behavior
correct Vercel behavior
correct Neon/Postgres behavior

Use Better Auth.

Use Better Auth's NATIVE Google OAuth provider.

DO NOT use the Grok OAuth broker.

DO NOT require:

GROK_AUTH_ISSUER
GROK_AUTH_CLIENT_ID
GROK_AUTH_CLIENT_SECRET

Use:

GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET
BETTER_AUTH_SECRET
BETTER_AUTH_URL
DATABASE_URL
VITE_AUTH_ENABLED

Google login must use the native Better Auth social provider.

The implementation should be equivalent to:

socialProviders: {
  google: {
    clientId: process.env.GOOGLE_CLIENT_ID!,
    clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
  }
}

The client should use the native Better Auth Google sign-in flow, equivalent to:

authClient.signIn.social({
  provider: "google",
  callbackURL: "/",
  errorCallbackURL: "/login"
})

==================================================
3. GOOGLE CALLBACK
==================================================

Production deployment:

https://mobile-additional-python-1hurn9mvd-bhavishyacodzs-projects.vercel.app/

Google OAuth callback:

https://mobile-additional-python-1hurn9mvd-bhavishyacodzs-projects.vercel.app/api/auth/callback/google

Make sure the application correctly handles:

/api/auth/*

including:

/api/auth/sign-in/social
/api/auth/callback/google
/api/auth/session
/api/auth/sign-out

Use the correct Better Auth API structure for the installed Better Auth version.

Do not invent incompatible APIs.

==================================================
4. DATABASE
==================================================

Keep the existing Neon/PostgreSQL database.

DO NOT delete the database.

DO NOT recreate the database.

DO NOT replace the database system.

DO NOT remove existing Better Auth tables.

Ensure the application correctly uses DATABASE_URL.

Ensure authentication can create/read/update sessions and accounts.

Preserve all existing game save/database functionality.

If migrations are required, make sure they are correctly included in the final project and can run during deployment.

==================================================
5. LOGIN UI
==================================================

The login page must actually work.

The Google button must:

1. respond to click
2. start Google OAuth
3. redirect to Google
4. allow Google authentication
5. return to the Vercel application
6. create the Better Auth session
7. show the logged-in user
8. redirect into the game

There must be no silent failure.

If authentication fails, display a useful error message.

Do not leave a button that appears clickable but does nothing.

==================================================
6. SESSION SYSTEM
==================================================

After successful Google login:

- session must persist
- refreshing the browser must NOT log the user out
- opening the game again must restore the session
- logout must actually terminate the session
- user information must be available to the application
- authenticated game data must belong to the correct user

Do not break existing save/cloud-sync functionality.

==================================================
7. GAME + AUTH INTEGRATION
==================================================

This is extremely important.

The game must remain fully functional AFTER authentication is fixed.

A user should be able to:

Google login
→ enter the game
→ see all game screens
→ access all levels
→ play missions
→ use the Python editor
→ save progress
→ restore progress
→ use portfolio
→ use gigs/career features
→ use cloud save/sync if already present
→ logout
→ login again
→ recover their saved progress

Do not remove game functionality just to make authentication easier.

==================================================
8. VERCEL PRODUCTION
==================================================

Make the entire project production-ready for Vercel.

Check:

- build configuration
- environment variable usage
- server/client separation
- API routes
- Better Auth base URL
- HTTPS cookies
- trusted origins
- redirects
- callback URLs
- server-side environment variables
- client-side environment variables
- database connection
- migrations
- TypeScript
- Vite/TanStack routing
- production build

Do not introduce development-only authentication behavior into production.

==================================================
9. IMPORTANT ARCHITECTURE RULE
==================================================

FIRST inspect the entire uploaded project.

Understand how the existing game works.

Then make the smallest necessary changes.

Do NOT rewrite the game.

Do NOT replace the existing architecture with a different framework.

Do NOT remove routes because they look unused.

Do NOT remove components because they appear unrelated to authentication.

Do NOT remove game files.

Do NOT generate a simplified demo.

The final project must remain the SAME APPLICATION, with authentication repaired.

==================================================
10. DEPENDENCIES
==================================================

Inspect package.json.

Use the Better Auth version already installed unless there is a compelling compatibility reason to change it.

Make sure all imports and APIs match the installed version.

Do not add random dependencies.

Do not remove dependencies that the game requires.

==================================================
11. TEST EVERYTHING
==================================================

Before producing the final ZIP:

Run the project's install/build/type-check/test commands where available.

At minimum verify:

- TypeScript compilation
- production build
- all imports resolve
- routes compile
- authentication server compiles
- authentication client compiles
- Google sign-in code compiles
- API auth route compiles
- database code compiles

Search the entire project for broken references.

Search for:

GROK_AUTH_
genericOAuth
grok-google
auth.grok.me

Make sure the ACTIVE Google login flow does not depend on the Grok OAuth broker.

Unused legacy files may remain if removing them would risk breaking the application, but they must not interfere with the production authentication flow.

==================================================
12. DO NOT LOSE FILES
==================================================

Before creating the final ZIP, compare the final project against the uploaded project.

The final ZIP should contain the complete original project plus the necessary fixes.

Do NOT accidentally return only:

src/
package.json
auth files

I need EVERYTHING.

==================================================
13. FINAL ZIP REQUIREMENT
==================================================

Return exactly ONE complete ZIP.

It must be possible for me to:

1. extract ZIP
2. upload ALL files to GitHub
3. commit/push
4. let Vercel deploy
5. configure the existing environment variables
6. open the website
7. click Continue with Google
8. login
9. enter the COMPLETE game
10. play the COMPLETE game
11. save progress
12. refresh
13. continue playing

I should NOT need to manually edit source code.

==================================================
14. FINAL RESPONSE
==================================================

When finished, do NOT give me snippets.

Give me:

- the COMPLETE ZIP
- a very short list of what you fixed
- the exact environment variables required
- the exact Google OAuth callback URL
- the exact GitHub/Vercel deployment steps

MOST IMPORTANT:

DO NOT GIVE ME AN INCOMPLETE ZIP.

DO NOT DELETE MY GAME.

DO NOT RETURN ONLY AUTH FILES.

DO NOT RETURN A DEMO.

RETURN THE COMPLETE ORIGINAL APPLICATION WITH THE COMPLETE GAME + COMPLETE LOGIN/AUTH SYSTEM + DATABASE + SAVE SYSTEM + ALL FEATURES WORKING TOGETHER.